const config = {
  host: "localhost",
  user: "root",
  database: "hosting",
  password: "106ee14002",
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
};

module.exports = config;
